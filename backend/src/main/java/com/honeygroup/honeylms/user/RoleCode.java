package com.honeygroup.honeylms.user;

/**
 * Business-level role codes, matching the values inserted by V1__create_role.sql.
 * The code should always reference these constants rather than raw strings.
 */
public enum RoleCode {
    STUDENT,
    TRAINER,
    ADMIN
}
